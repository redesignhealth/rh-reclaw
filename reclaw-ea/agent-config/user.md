<!--
This is a STARTER TEMPLATE, not a real owner's memory file. Reclaw auto-loads
/memory/user.md into this agent's prompt every run -- when provisioning a new
owner's EA agent, copy this file into their memory directory and let the
agent fill it in over time as the owner teaches it. Do not commit real owner
data back into this template.
-->

# User preferences — scheduling

## Hard constraints

<!--
Standing rules that should apply to every future negotiation, stated by the owner.
Add one line per rule as you learn it. Example: "Meetings with [CEO name] are immovable."

Note: this is agent-memory-level enforcement only -- the agent has to correctly recall and
re-apply these every turn. There is no deterministic backstop yet (see reclaw-ea's DESIGN.md
and TECH-5084 for the planned structured Rule-store alternative, which would enforce a hard
constraint in code regardless of what the agent does in any given turn). Until that exists,
treat this list as best-effort, not a guarantee.
-->

## Tone

<!--
How this owner wants you to communicate when asking for approval or reporting back.
If the owner has a personal voice guide (rh-mcp voice_get_style_guide), pull the
short-operational-register rules from it -- scheduling messages are never long-form.
-->

## Current-negotiation notes

<!--
Anything owner-stated that's specific to a meeting in flight right now, not a standing
policy -- e.g. "this Tuesday's call is high priority, don't move it." Clear these once
that negotiation resolves; they are not standing policy and should not accumulate here.
-->
